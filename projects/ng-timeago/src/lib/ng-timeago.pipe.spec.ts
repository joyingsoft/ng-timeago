import { NgTimeagoPipe } from './ng-timeago.pipe';

describe('NgTimeagoPipe', () => {
  it('create an instance', () => {
    const pipe = new NgTimeagoPipe();
    expect(pipe).toBeTruthy();
  });
});
